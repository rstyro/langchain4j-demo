# LangChain4j-Agentic — Supervisor 自主智能体
spring:
  application.name: agent-supervisor

langchain4j:
  open-ai:
    chat-model:
      api-key: ${AI_API_KEY:}
      base-url: https://ark.cn-beijing.volces.com/api/coding/v3
      model-name: deepseek-v3.2
      temperature: 0.2
      log-requests: true
      log-responses: true

server:
  port: 8106
